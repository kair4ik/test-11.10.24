2. Ниже представлена иерархия классов. Какой прием ООП применяется при вызове метода go()?
abstract class Car
{
    abstract function go();
}

class RaceCar extends Car{
    public function go()
    {
        return '280 km/h';
    }
}

class Track extends  Car{
    public function go()
    {
        return '80 km/h';
    }
}
$queue = [];
$queue[] = new Track();
$queue[]  = new RaceCar();

foreach($queue as $row){
    echo $row->go();
}


2. Полиморфизм.

3. В чем отличие SQL от MySQL?

3. SQL это язык запросов, а MySQL это система управления базами данных, которая использует SQL для выполнения операций.

4. Сделайте реализацию паттерна Singelton. Посмотрите, можно ли его применить в первом задании.

4. Реализовал в самом тестовом задании.